# Generado por theme-changer — no editar directamente

general {
    col.active_border   = rgba(94e2d5ff) rgba(a6e3a1ff) 45deg
    col.inactive_border = rgba(1e1e2eaa)
}

decoration {
    shadow {
        color = rgba(000000aa)
    }
}

# Blur de capas
blurls = wlogout
blurls = rofi
